profiles {
    upside_down {
        params.reverse_sort = true
    }

    cc {
        executor: 'slurm'
        withName: "ConvertBamToSam" {
            module 'samtools/1.6'
        }
    }

    local {
        docker: true
        container: 'ruby'
    }
}